# Core dependencies (prefer apt packages when available)
# Install via apt: sudo apt install python3-opencv python3-numpy python3-picamera2
# Fallback pip packages below:

opencv-python>=4.8.0
numpy>=1.24.0

# Development dependencies (pip only)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0