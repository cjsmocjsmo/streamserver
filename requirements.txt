# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0

# Raspberry Pi camera (install via apt on Raspberry Pi)
# picamera2 - install with: sudo apt install python3-picamera2

# Development dependencies (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0